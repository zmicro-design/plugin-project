#!/bin/bash

project::release() {
  # apply proxy
  net::using_smart_proxy

  # zmicro
  if [ -f "$PWD/mod" ]; then
    zmicro release .
    return $?
  fi

  # others: node.js, go ...
  os::gpm release
}

export -f project::release
