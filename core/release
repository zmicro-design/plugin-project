#!/bin/bash

project::release() {
  # apply proxy
  net::using_smart_proxy

  zmicro release .
  if [ "$?" != "0" ]; then
    log::error "[$(timestamp)] failed to release project."
    return 1
  fi
}

export -f project::release
